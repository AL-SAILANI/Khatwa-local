# Roadmap — خطوة (Khatwa)

## تم إنجازها (Built)

- **نظام التصميم**: رموز الألوان، الوضع الداكن القائم على الكلاس (class-based)، RTL، الطباعة المعتمدة على اللغة (`src/app/globals.css`, `src/lib/fonts.ts`). الخط المرخص "خط ثمانية" (Thmanyah) هو الخط الرسمي للعربية — Thmanyah Sans للنصوص/UI، Thmanyah Serif Display للعناوين مطبقة على كل العناوين (`h1`–`h6`) — بينما الإنجليزية تستخدم Plus Jakarta Sans (خط rounded sans مفتوح المصدر وجريء) لكليهما، مخصص عبر `html[lang="en"]`. وجه رابع، Thmanyah Serif Text، محمل للاستخدام المستقبلي في النصوص الطويلة.
- **التدويل (i18n)**: العربية افتراضياً + ترجمة إنجليزية كاملة، توجيه المسارات (`src/i18n/`, `messages/`)، مفتاح لغة في كل شريط علوي (`src/components/theme/language-toggle.tsx`). كل الشاشات — لوحة التحكم، الدورات، المفردات، الامتحانات التجريبية، اختبار تحديد المستوى، خطة الدراسة، الإعدادات، الإنجازات، المتصدرين، الإدارة، محرك الامتحان، التسعير، نماذج المصادقة ورسائل التحقق من Zod — تستخدم `useTranslations`/`getTranslations` بدلاً من النصوص العربية المكتوبة يدوياً. أسماء الأيام/الأشهر والتنسيق النسبي للوقت تستخدم `Intl.DateTimeFormat`/`Intl.RelativeTimeFormat` بدلاً من المصفوفات العربية المكتوبة يدوياً (`src/lib/format.ts`). محتوى الدورس/المسارات مترجم بالكامل للغة الإنجليزية في `messages/en.json` تحت `content.*` (عناوين + ملاحظات الدروس الـ55 ونصوص المسارات الـ8)، مع تراجع آمن للقيمة العربية المخزنة في Firestore عند غياب المفتاح (`src/hooks/use-localized-content.ts`). نصوص بنك الأسئلة تبقى إنجليزية لأنها اللغة المختبرة.
- **أساسيات Firebase**: SDK العميل، SDK الإدارة، مساعدات المصادقة، قواعد الأمان، الفهارس، إعداد App Hosting
- **نموذج بيانات Firestore** كنماذج TypeScript (`src/types/`)
- **مكتبة UI مشتركة**: Button, Card, Badge, Input, Container/Section, Logo (`src/components/ui`, `src/components/brand`)
- **صفحة الهبوط (Landing)**: Hero, Stats, Features, Why Us, Testimonials, FAQ, CTA, Footer, Navbar. تصميم بصري يستخدم تدرج teal-to-mint بامتداد كامل مع نمط hatch قطري أصلي (`src/components/landing/decorative-hatch.tsx`) و blob SVGs زخرفية (`src/components/landing/decorative-blob.tsx`)، لوحة ألوان تكميلية بنفسجي/عنبري/نعناعي بجانب الأزرق الفيروزي الأساسي للعلامة التجارية (`src/app/globals.css`)، خط عرض bold rounded (Plus Jakarta Sans, `--font-heading` في `src/lib/fonts.ts`) للعناوين — العناوين العربية تسقط تلقائياً على Cairo لأن خط العرض لا يحتوي على حروف عربية. جميع الأصول أصلية، لا صور أو خطوط خارجية.
- **المصادقة**: تسجيل الدخول، التسجيل (مع اختيار الهدف)، نسيت كلمة المرور، تسجيل دخول Google مع خطوة onboarding لاختيار الهدف — كلها مدعومة بقراءات/كتابات Firestore حقيقية (`src/lib/firestore/users.ts`)
- **لوحة التحكم**: إحصائيات حقيقية (المستوى، السلسلة، XP، الامتحانات، آخر نتيجة)، تقدم الهدف، أدوات التقويم/السلسلة مدفوعة بسجل نشاط غير طبيعي، غلاف التطبيق sidebar/topbar مع حارس المصادقة، بطاقة "متابعة التعلم" تحل الدرس غير المكتمل التالي في آخر دورة لمسها الطالب (تسقط على الدورة التالية التي لا يزال بها شيء للدراسة، `src/lib/continue-learning.ts`)، وبطاقة "مدرب الدراسة" المبنية على القواعد توصي بالدروس التالية من تاريخ تقدم الامتحان/الدرس
- **بنك الأسئلة + نصوص القراءة**: 256 سؤالاً أصلياً عبر أقسام STEP الأربعة (45 قواعد، 87 قراءة، 86 استماع، 38 تحليل كتابي) مع **27 نص قراءة** (منها 15 نصاً جديداً من موضوعات القطع المتوقعة في المنهج المرجعي: الفضاء، الزلازل، النحل، صناعة الورق، جسر الملك فهد، ابن سينا، الآيس كريم، الإنترنت في السعودية، الحرب العالمية الثانية، إسطنبول، تلوث الهواء، محمد علي، العملات، السكر، الحمض النووي) و 86 حوار استماع — كل قسم يتجاوز احتياج الاختبار التجريبي الرسمي (قراءة 40، قواعد 30، استماع 20، كتابة 10). أدوات وصول Firestore، `scripts/seed.ts`. المحتوى مبني على المرجع العلمي المعتمد `step-prep-curriculum` (`.opencode/skills/step-prep-curriculum/`).
- **صوت الاستماع (TTS مجاني)** — ملفات MP3 مولّدة لجميع حوارات الاستماع (86 سؤالاً / 29 نصاً فريداً) عبر **Microsoft Edge TTS** (`edge-tts`): مجاني بالكامل، بلا مفاتيح ولا فوترة ولا Firebase Storage. أصوات متمايزة رجالية/نسائية لكل متحدث تُدمج بفواصل طبيعية عبر ffmpeg (`scripts/generate-listening-audio.py`)، وتُحفظ في `public/audio/listening/` وتُخدم كملفات ثابتة. كل سؤال استماع في Firestore يشير إليها عبر `audioUrl`، وواجهة السؤال تشغّلها بمشغّل `<audio controls>` (`src/components/exam/question-view.tsx`) بدلاً من عرض النص. إعادة التوليد عند تعديل النصوص: `python3 scripts/generate-listening-audio.py` (يتخطى الموجود إلا بـ`--force`). (خيار ترقية مستقبلي اختياري: Cloud Text-to-Speech من Google لاحتياج خطة Blaze.)
- **محرك الامتحان المشترك**: مؤقت بملء الشاشة، تنقل الأسئلة، تعليم، مراجعة قبل الإرسال، تتبع الوقت لكل قسم، تقييم موزع مطابق لتوزيع STEP (`src/components/exam/`, `src/lib/exam-scoring.ts`)
- **اختبار تحديد المستوى (20 سؤالاً عبر جميع الأقسام)** → صفحة نتائج/تحليلات → مولد خطة دراسة (توصية مدة 40/60/90 يوماً، جدول أسبوعي نموذجي)
- **امتحان تجريبي كامل (100 سؤال / 165 دقيقة مطابق لتوزيع أقسام STEP)** يشارك نفس المحرك وصفحة النتائج كاختبار تحديد المستوى (`useExamSession`); الحسابات المجانية تحصل على محاولة كاملة واحدة، ثم ترى ترقية للاشتراك (`src/components/mock-exams/mock-exam-list.tsx`)
- **الدورات والدروس**: كتالوج، قائمة دروس لكل دورة مع حالة الإكمال (55 درساً: 14 نحو، 8 لكل مسار قراءة/استماع/كتابة/مفردات، 3 لكل مسار نصائح/استراتيجيات/مراجعة)، عارض الدروس مع ملاحظات + اختبار مضمّن + وسائط فيديو/صوت/PDF حقيقية + مشغّل صوت للحوارات النموذجية في دروس الاستماع، تتبع `lessonProgress`. ملاحظات الدروس كلها بقالب تعليمي علمي موحد (القاعدة المركزية / الصيغة السريعة / فخ STEP / مراجعة متباعدة) مع **تكنيكة اسما (ASMA) للقراءة** في كل دروس القراءة الثمانية و**تكنيكة اجدا (AGDA) + قاعدة الـ45 ثانية** في كل دروس القواعد الأربعة عشر. CMS الإدارة يرفع الموارد مباشرة إلى Firebase Storage (`src/lib/firebase/storage-upload.ts`)
- **بطاقات المفردات الفلاش**: تكرار متباعد SM-2 حقيقي (`src/lib/spaced-repetition.ts`)، مجموعة بداية 626 كلمة (كلمات أكاديمية، سيناريوهات استماع، مفردات نصوص القراءة الجديدة) **مصنّفة حسب المستوى (230 سهلة / 258 متوسطة / 138 صعبة)** عبر `src/data/vocab-levels.ts` مع شارة مستوى وفلترة في المتصفح، قائمة مراجعة يومية تخلط الكلمات المستحقة/الجديدة، متصفح قابل للبحث مع إشارات مرجعية
- **محرك المفردات الذكي العلمي** — مشغّل جلسة موحّد (`src/components/vocabulary/flashcard-session.tsx` + `session-summary.tsx`) يجمع أبرز نتائج علم التعلم: **استرجاع نشط باتجاهين** يتخلل تلقائيًا ~ثلث البطاقات في وضع الإنتاج معنى→كلمة (أثر التوليد، Slamecka & Graf) والباقي تعرفٍ كلمة→معنى (أثر الاختبار، Roediger & Karpicke)، **تداخل جديد/مستحق** في الطابور اليومي (Interleaving، Rohrer & Taylor / Dunlosky) بدل التجميع، **إعادة التثبيت داخل الجلسة** للكلمات المنسية بنمط Leitner (ترجع لنهاية الطابور الآن لا غدًا)، شفافية فاصلة SM-2 بعد كل إجابة («ستُعرض بعد ٦ أيام»)، اختصارات لوحة ١/٢/٣، مكافأة XP رمزية لكل جلسة، ومتتبع نسيان (lapses) في `vocabReviewState`. **ملخص الجلسة** يعرض نسبة الاستبقاء بمنحنى إبنجهاوس المقارن (مراجعة مقابل نسيان)، جدول المراجعة القادم، ونصائح منهجية موثقة من الدراسات. المتصفح يعرض حالة كل كلمة (جديدة/مستحقة اليوم/بعد عدة أيام).
- **منهج المفردات المترابط (Vocab Curriculum)** — `src/lib/vocab-curriculum.ts` (مختبر): يبني تسلسلاً منهجياً حتمياً لكل الـ626 كلمة بثلاث طبقات: **(1)** صعوبة تدريجية easy→medium→hard (البداية خفيفة والتحدي يتصاعد يوماً بيوم)، **(2)** تداخل أقسام STEP الأربعة داخل كل مستوى (تنوع يومي كالاختبار الحقيقي)، **(3)** تثبيت كل كلمة على «درسها الأم» في دورة قسمها عبر round-robin مشتقّ من البيانات (مثل `tag-lesson-map`) فيبقى الربط صالحاً مع تطوّر الدروس لاحقاً. **التخصيص اليومي من الخطة**: عدد الكلمات الجديدة يومياً = `ceil(إجمالي/مدة الخطة)` في حدود 5–25 (`wordsPerDay`)، مع تقدم يومي يُحسب من `startedAt` الخطة (`planDayIndex`)، وطابور المراجعة يعطي أولوية للمستحقة (استبقاء SM-2) ثم يأخذ حصة اليوم من كلمات المنهج مع تعويض الأيام الفائتة. البطاقة تعرض شارة «من درس: …» تربطها بدرسها وتؤدي إليه مباشرة.
- **الألعابية (Gamification)**: مستويات مبنية على XP، فتح الإنجازات مع مكافآت XP حقيقية، شبكة إنجازات كاملة، متصدرون مدعومون بمجموعة `publicProfiles` غير طبيعية
- **الإعدادات**: الوضع الداكن، تبديل الإشعارات موصول بتسجيل توكن Web Push (FCM) حقيقي، تبديل اللغة، تغيير كلمة المرور، حذف الحساب (الكل مع إعادة مصادقة Firebase صحيحة)
- **CMS إدارة أساسي** (`/admin`, محمي بمطالبة Firebase custom claim عبر `scripts/set-admin-claim.ts`): إحصائيات المنصة، إدارة مستوى خطة المستخدم، تأليف الدورات/الدروس مع رفع وسائط Storage، محرر بنك الأسئلة
- **أساسيات PWA**: `app/manifest.ts`، service worker يدوي للـ offline-shell (`public/sw.js`, مسجل في الإنتاج فقط)، بيانات iOS home-screen
- **الحذف المتتالي عند إزالة الحساب** — يعمل من جانب العميل، لا حاجة Cloud Functions/خطة Blaze: `deleteAccount` (`src/lib/firebase/auth.ts`) يستدعي `deleteUserData` (`src/lib/firestore/users.ts`)، الذي ينظف `examAttempts`, `lessonProgress`, `studyPlans`, `userAchievements`, `vocabReviewState` لذلك المستخدم (بالإضافة إلى `publicProfiles`/`users`) قبل إزالة حساب Auth نفسه. `firestore.rules` يمنح owner-delete على كل من هذه المجموعات تحديداً للسماح بذلك. نسخة Cloud Functions (`functions/`) لا تزال موجودة كمسار ترقية اختياري — انظر `functions/README.md` — لأنها تغطي أيضاً الحسابات المزالة مباشرة من وحدة تحكم Firebase، والتي لا يستطيع تدفق جانب العميل التقاطها.
- **الإشعارات** — Web Push (FCM) موصول بالكامل: تبديل الإعدادات يطلب الإذن ويسجل توكن (`src/lib/firebase/messaging.ts`)، مسار service-worker ديناميكي (`src/app/firebase-messaging-sw.js/route.ts`) يتعامل مع الإشعارات في الخلفية، وتذكير السلسلة اليومي يُرسل بواسطة **Vercel Cron job** (`vercel.json`) يضرب `src/app/api/cron/streak-reminder`، باستخدام Admin SDK مباشرة — لا حاجة Cloud Functions. لا يزال يحتاج مفتاح VAPID لـ Web Push (`NEXT_PUBLIC_FIREBASE_VAPID_KEY`) لإرسال أي شيء فعلياً — انظر "الفجوات المعروفة."
- **الاشتراكات** — صفحة تسعير حقيقية (`/pricing`) بالإضافة إلى scaffold لـ Stripe Checkout (`src/lib/stripe/`, `src/app/api/stripe/`) يرفع `planTier` عبر webhook. مبرمج ومنمط بالكامل؛ يحتاج مفاتيح Stripe الخاصة بمالك المشروع لشحن أي شخص فعلياً — انظر "الفجوات المعروفة."
- **مدرب دراسة AI** — مطبق كموصي مبني على القواعد، deterministic (`src/lib/study-coach.ts`), ليس استدعاء LLM: يرتب أقسام STEP حسب النسبة المئوية الصحيحة المرجحة بالأحدث عبر المحاولات الحديثة، ويوصي بالدرس غير المكتمل التالي لكل مسار ضعيف. لا يحتاج مفتاح API خارجي، ويعمل بالفعل ضد تاريخ الامتحان الحقيقي.
- **CI** — `.github/workflows/ci.yml` يحتوي على وظيفتين: lint/typecheck/build الأصلي، بالإضافة إلى وظيفة `e2e` تشغل اختبارات Playwright الدخانية (`e2e/smoke.spec.ts`) ضد مجموعة محاكي Firebase (`firebase emulators:exec`) — لا أسرار مشروع مطلوبة، لأن `NEXT_PUBLIC_USE_FIREBASE_EMULATOR=true` يوجه SDKs العميل للمحاكيات المحلية (`src/lib/firebase/client.ts`). وظيفة `unit` منفصلة الآن تشغل اختبارات Vitest (115 اختباراً عبر 9 ملفات) في كل PR/push (`npm run test:unit`).
- **دائرة التعلم المغلقة (سؤال ← مفهوم ← درس)** — فهرس وسوم→دروس يُشتق من بنك الأسئلة والدروس بدلاً من الصيانة اليدوية (`src/lib/tag-lesson-map.ts`): 109/110 سؤالاً مرتبطاً بـ 40 وسماً. مراجعة ما بعد الامتحان ورقائق "المهارات الضعيفة" في النتائج وشرح التحدي اليومي كلها تصل درساً فعلياً لدراسته، مع تطبيع المرادفات (`dangling-modifiers→dangling-modifier`, `parallelism→parallel-structure`).
- **عرض الدروس العلمي المنظم** (`src/components/courses/lesson-notes.tsx`) — ملاحظات كل درس تُعرض كبطاقات ملونة مصنفة برموزها: 📌 القاعدة المركزية، ⚡ الصيغة السريعة، ⚠️ فخ STEP، 🔄 المراجعة المتباعدة، 🧭 تكنيكة اسما/اجدا (بأيقونات وألوان مميزة)، مع بطاقة تنبيه ⏱️ لقاعدة الـ45 ثانية — بدلاً من كتلة نصية واحدة.
- **تحليلات النتائج المرئية** — بطاقة أداء الأقسام الأربعة في لوحة التحكم (`section-performance-card.tsx`) بترجيح المحاولات الأحدث، منحنى تطور النتائج SVG في صفحة النتائج (`score-trend.tsx`) عبر آخر 10 محاولات، ووقت/سؤال لكل قسم في تفصيل النتائج.
- **التحدي اليومي المتكيف** (`daily-challenge-card.tsx`) — يسحب سؤالاً حقيقياً من أضعف قسم لديك (مرتبة عبر المحاولات الحديثة)، مع شرح وربط دروس، ومكافأة +30 XP لليوم.
- **التقويم الحراري 90 يوماً + السلسلة** — إكمال درس أو إتمام التحدي اليومي يحسب نشاطاً (`markDayActive` في `src/lib/firestore/users.ts`)، وفتح إنجاز "أسبوع متتالي" عند 7 أيام.
- **النطق الصوتي للمفردات (TTS مجاني)** — ملفات MP3 مولّدة عبر edge-tts لكل كلمة من الـ626 (`public/audio/vocab/`, `scripts/generate-vocab-audio.py`)، يُفضّلها زر النطق (`pronounce-button.tsx`) مع تراجع لـ speechSynthesis للملفات غير المولدة.
- **أمان المصادقة** — نموذج تسجيل الدخول لم يعد يخزن كلمة المرور في localStorage إطلاقاً (يخزن البريد فقط في `khatwa.remembered-email`؛ الحفظ عبر `setPersistence`).
- **حالات فارغة مصقولة** — كتالوج الدورات والبطاقات الجديدة تعرض رسائل مخصصة مع CTA بدلاً من شبكة فارغة.
- **صفحات الخطأ والأمور الخاصة بـ App Router** — ملفات convention كاملة: `[locale]/error.tsx` (عميل، يستخدم `unstable_retry` في Next 16 بدلاً من `reset`)، `app/global-error.tsx` الجذر، `[locale]/not-found.tsx` (404 محلية مع مفاتيح `notFound` في `messages/`)، و`[locale]/loading.tsx` — بجانب `PageErrorBoundary` من جانب العميل. إصلاح خطأ كامن في `Button asChild`/`Slot`: عند `asChild` يمرر زر فقط child عنصر واحد لـ Radix Slot (بدون عنصر السبينر الإضافي) — كانت `not-found.tsx` أول استخدام فعلي لهذا المسار وكشفت الخطأ.

---

## الفجوات المعروفة (Real, not yet done)

- **مشروع Firebase متصل** — `.env.local` مربوط بمشروع `khotwa-2026` (يحتوي المحتوى المسقوف)، وتم نشر `firestore.rules` + `firestore.indexes.json` المحدّثين إليه. إعداد Firebase Storage ما زال معلقاً (زر "Get Started" في Firebase Console > Storage مطلوب لرفع وسائط الدورات عبر CMS).
- **الإشعارات تحتاج مفتاح VAPID.** كل شيء موصول من النهاية للنهاية، لكن `NEXT_PUBLIC_FIREBASE_VAPID_KEY` فارغ حتى تولد شهادة Web Push في Project Settings > Cloud Messaging. (مجاني — لا فوترة/بطاقة، مجرد زر في الكونسول.)
- **`CRON_SECRET` غير مضبوط على Vercel بعد.** مسار cron لتذكير السلسلة يعمل بدونه (يسقط للسماح بأي طلب)، لكن ضبط `CRON_SECRET` على أي سلسلة عشوائية في متغيرات بيئة مشروع Vercel يقفل نقطة النهاية لجدول Vercel الخاص.
- **Stripe يحتاج مفاتيح حقيقية.** `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `STRIPE_PRICE_ID_PRO`, `STRIPE_PRICE_ID_PREMIUM` كلها فارغة؛ صفحة التسعير تظهر خطأ "not configured yet" واضح بدلاً من التعطل حتى تُضبط.
- **عمق المحتوى** — 256 سؤالاً (27 نص قراءة)، 55 درساً، و 626 كلمة مفردات: البنك يغطي عدة امتحانات تجريبية كاملة (كل قسم يتجاوز حاجته بعدة مرات)، لكن التنوّع للاختبارات المتكررة لا يزال قابلاً للنمو — استخدم CMS الإدارة أو `scripts/seed.ts` لمواصلة نموه. ملاحظة: seeding المشروع الحي يتطلب `FIREBASE_SERVICE_ACCOUNT_KEY` (أو `firebase login` مصادق).
- **تحليلات الإيرادات/الاستخدام** — نظرة الإدارة تظهر أعداد المنصة (مستخدمين، محاولات امتحان، دورات، أسئلة) لكن لا بيانات إيرادات حقيقية بعد، لأن Stripe غير موصول بحساب حي.

---

## خارطة طريق التطوير المحسّنة (Enhanced Development Roadmap)

### 🎯 الأولوية القصوى — للإطلاق (Launch-Critical)

| # | المجال | المهمة | الوصف | الجهد |
|---|--------|--------|---------|------|
| 1 | **الإشعارات** | إنشاء مفتاح VAPID | توليد مفتاح Web Push في Firebase Console وإضافته كـ `NEXT_PUBLIC_FIREBASE_VAPID_KEY` | ⏱️ 5 د |
| 2 | **Cron** | ضبط `CRON_SECRET` | توليد سر عشوائي وإضافته في Vercel Environment Variables | ⏱️ 2 د |
| 3 | **المدفوعات** | مفاتيح Stripe الحية | إنشاء منتجات/أسعار Stripe، إضافة المفاتيح، اختبار تدفق checkout كامل | ⏱️ 30 د |
| 4 | **المحتوى** | التوسع إلى ~100 سؤال | إضافة 30+ سؤالاً عبر جميع الأقسام الأربعة، نصوص قراءة/استماع إضافية | ⏱️ 4-8 س |
| 5 | **المحتوى** | إكمال مكتبة الدروس | 3 دروس لكل مسار × 8 مسارات = 24 درساً (تم جزئياً)، إكمال الوسائط | ⏱️ 8-16 س |
| 6 | **المحتوى** | توسيع المفردات | 200+ كلمة مع معاني، أمثلة، صوت | ⏱️ 4-8 س |

### 🔧 تحسينات تقنية وجودة (Technical Quality)

| # | المجال | المهمة | الوصف | الجهد |
|---|--------|--------|---------|------|
| 7 | **الاختبارات** | توسيع تغطية Playwright | إضافة اختبارات: تدفق الامتحان كاملاً، مراجعة المفردات، إكمال الدروس، لوحة التحكم | ⏱️ 4-8 س |
| 8 | **الاختبارات** | اختبارات الوحدة (Vitest) | إضافة اختبارات للوظائف الخالصة: scoring, spaced-repetition, study-coach, calendar | ⏱️ 4-8 س |
| 9 | **الأداء** | تحسين حزم JavaScript | تحليل الحزمة، code-splitting للصفحات الثقيلة (exam-runner, admin) | ⏱️ 2-4 س |
| 10 | **الأداء** | استعلامات Firestore | إضافة فهارس مركبة مفقودة، تقليل القراءة الزائدة في `useUserProfile` | ⏱️ 2-4 س |
| 11 | **إمكانية الوصول** | تدقيق WCAG 2.1 AA | ألوان التباين، ترميز ARIA، التنقل بالكيبورد، قارئات الشاشة | ⏱️ 4-8 س |
| 12 | **الأخطاء** | حدود خطأ React | إضافة Error Boundaries للصفحات الحرجة، صفحة خطأ ودية | ⏱️ 2-4 س |
| 13 | **المراقبة** | Sentry / LogRocket | تتبع الأخطاء، جلسات المستخدم، مقاييس الأداء | ⏱️ 1-2 س |
| 14 | **النوع (TypeScript)** | strict mode كامل | تفعيل `strict: true`، إزالة `any`، أنواع صارمة لـ Firestore data | ⏱️ 2-4 س |

### 🎨 تجربة المستخدم والواجهة (UX/UI Polish)

| # | المجال | المهمة | الوصف | الجهد |
|---|--------|--------|---------|------|
| 15 | **الهبوط** | قسم الدليل الاجتماعي | إضافة شعارات شركاء، شهادات فيديو، دراسات حالة حقيقية | ⏱️ 2-4 س |
| 16 | **الهبوط** | عرض تفاعلي | فيديو/عرض توضيحي تفاعلي لمحرك الامتحان، بطاقات الفلاش | ⏱️ 4-8 س |
| 17 | **التسجيل** | تبسيط التدفق | تقليل الحقول، تسجيل دخول اجتماعي إضافي (Apple، Microsoft) | ⏱️ 2-4 س |
| 18 | **لوحة التحكم** | حالات فارغة أفضل | رسوم توضيحية، CTAs للسياقات الفارغة (لا خطة، لا اختبارات، لا أنشطة) | ⏱️ 2-4 س |
| 19 | **لوحة التحكم** | عرض تقدم بصري | مخططات للمناطق الضعيفة، heatmap للسلسلة، رسم بياني لتقدم XP | ⏱️ 4-8 س |
| 20 | **الامتحان** | مراجعة ما بعد الامتحان | شرح تفصيلي لكل سؤال، ربط بالدرس المرتبط، زر "دراسة هذا المفهوم" | ⏱️ 8-16 س |
| 21 | **المفردات** | نطق صوتي | دمج TTS أو ملفات صوتية مسجلة للكلمات والأمثلة | ⏱️ 4-8 س |
| 22 | **المفردات** | مجموعات مخصصة | السماح للمستخدمين بإنشاء مجموعات مفردات خاصة بهم | ⏱️ 4-8 س |
| 23 | **الألعابية** | شرائح المتصدرين | أسبوعي/شهري/كل الوقت، تصفية حسب المستوى، مشاركة اجتماعية | ⏱️ 4-8 س |
| 24 | **الإعدادات** | تصدير البيانات | GDPR compliance — تصدير JSON لكل بيانات المستخدم | ⏱️ 2-4 س |
| 25 | **PWA** | تثبيت موجه | نافذة تثبيت مخصصة، أيقونات شاشة البداية، اختصارات التطبيق | ⏱️ 2-4 س |

### 🧠 ميزات ذكية و AI (Smart/AI Features)

| # | المجال | المهمة | الوصف | الجهد |
|---|--------|--------|---------|------|
| 26 | **مدرب الدراسة** | ترقية LLM | دمج OpenAI/Anthropic لتوصيات شرحية، محادثات | ⏱️ 8-16 س |
| 27 | **تحليل الكتابة** | تصحيح AI | تقييم تلقائي لكتابة STEP، تعليقات على القواعد/البنية/المفردات | ⏱️ 16-24 س |
| 28 | **التحدث** | تدريب النطق | التعرف على الكلام، تقييم النطق، تمارين محاكاة التحدث | ⏱️ 16-24 س |
| 29 | **خطة متكيفة** | جدولة ديناميكية | تعديل الخطة بناءً على الأداء، إعادة جدولة ذكية | ⏱️ 8-16 س |
| 30 | **توليد أسئلة** | AI-assisted | توليد أسئلة/متغيرات جديدة لبنك الأسئلة بمساعدة LLM | ⏱️ 8-16 س |

### 🏗️ البنية التحتية و DevOps (Infrastructure)

| # | المجال | المهمة | الوصف | الجهد |
|---|--------|--------|---------|------|
| 31 | **الاستضافة** | Firebase App Hosting | ربط GitHub repo، تكوين البيئات (staging/prod) | ⏱️ 1-2 س |
| 32 | **قاعدة البيانات** | فهارس Firestore | مراجعة وتحسين جميع الفهارس المركبة، إزالة غير المستخدمة | ⏱️ 1-2 س |
| 33 | **الأمان** | مراجعة القواعد | تدقيق شامل لـ `firestore.rules` و `storage.rules` | ⏱️ 2-4 س |
| 34 | **النسخ الاحتياطي** | نسخ احتياطي مجدول | تصدير Firestore يومي/أسبوعي إلى Cloud Storage | ⏱️ 1-2 س |
| 35 | **CI/CD** | بيئة Staging | نشر تلقائي للـ preview deployments، اختبارات E2E عليها | ⏱️ 2-4 س |
| 36 | **الملاحظية** | لوحات Grafana/Datadog | مقاييس مخصصة: مستخدمين نشطين، معدل إكمال الامتحان، معدل التحويل | ⏱️ 2-4 س |

### 🌍 التوسع والنمو (Growth & Expansion)

| # | المجال | المهمة | الوصف | الجهد |
|---|--------|--------|---------|------|
| 37 | **اللغات** | واجهة مستخدم كاملة بالإنجليزية | ترجمة جميع مفاتيح `en.json`، اختبار RTL/LTR | ⏱️ 4-8 س |
| 38 | **اللغات** | لغات إضافية | دعم الفرنسية/الإسبانية/الأردية (خطوط، ترجمات، تواريخ) | ⏱️ 8-16 س |
| 39 | **الامتحانات** | اختبارات أخرى | IELTS، TOEFL، EMSAT — محركات منفصلة، محتوى منفصل | ⏱️ 40-80 س |
| 40 | **B2B** | لوحة مؤسسات | إدارة الفرق، التقارير المجمعة، SSO، فوترة موحدة | ⏱️ 40-80 س |
| 41 | **الهاتف** | React Native / Expo | تطبيق أصلي للموبايل مع مزامنة Offline-first | ⏱️ 80-160 س |
| 42 | **API** | API عامة | REST/GraphQL API للتكاملات الخارجية، webhooks | ⏱️ 16-24 س |

### 📚 توثيق ونقل معرفة (Documentation)

| # | المجال | المهمة | الوصف | الجهد |
|---|--------|--------|---------|------|
| 43 | **المطورين** | دليل المساهمة | `CONTRIBUTING.md`، معايير الكود، سير عمل PR | ⏱️ 2-4 س |
| 44 | **المطورين** | وثائق المعمارية | مخططات تدفق البيانات، قرارات التصميم (ADRs) | ⏱️ 4-8 س |
| 45 | **المحتوى** | دليل المؤلفين | كيفية إضافة أسئلة، دروس، مفردات عبر CMS الإدارة | ⏱️ 2-4 س |
| 46 | **المستخدمين** | مركز المساعدة | FAQs، أدلة فيديو، استكشاف الأخطاء | ⏱️ 8-16 س |

---

## أولويات مقترحة للسباقات القادمة (Sprint Priorities)

### Sprint 1 (الأسبوع 1-2): "إعداد الإطلاق"
- [ ] مفتاح VAPID + `CRON_SECRET` + مفاتيح Stripe
- [ ] توسيع المحتوى إلى 100 سؤال / 24 درس / 150 كلمة
- [x] Error Boundaries + صفحة خطأ ودية (convention files + `global-error.tsx`)
- [ ] تدقيق إمكانية الوصول الأساسي (التباين، ARIA، الكيبورد)

### Sprint 2 (الأسبوع 3-4): "الجودة والاختبارات"
- [ ] اختبارات Playwright موسعة (تدفقات حرجة 10+)
- [x] اختبارات وحدة Vitest للوظائف الخالصة (98 اختباراً + وظيفة CI)
- [ ] تحليل الحزمة + code-splitting
- [x] TypeScript strict mode

### Sprint 3 (الأسبوع 5-6): "تلميع UX"
- [x] حالات فارغة بلوحة التحكم/الكتالوج
- [x] مراجعة ما بعد الامتحان مع شروحات + ربط الدرس ("ادرس هذا المفهوم")
- [x] نطق صوتي للمفردات (edge-tts MP3)
- [ ] شرائح المتصدرين (أسبوعي/شهري)

### Sprint 4 (الأسبوع 7-8): "البنية التحتية"
- [ ] Firebase App Hosting + بيئة Staging
- [ ] مراجعة قواعد الأمان
- [ ] نسخ احتياطي مجدول
- [ ] Sentry للمراقبة

### Sprint 5+ (الأسبوع 9+): "الميزات الذكية والنمو"
- [ ] مدرب دراسة LLM
- [ ] تصحيح كتابة AI
- [ ] دعم امتحانات إضافية (IELTS/TOEFL)
- [ ] تطبيق موبايل أصلي

---

## مقاييس النجاح (Success Metrics)

| المقياس | الهدف عند الإطلاق | هدف 6 أشهر |
|----------|------------------|------------|
| مستخدمون مسجلون | 1,000 | 25,000 |
| معدل إكمال الاختبار التجريبي | >60% | >75% |
| معدل الاحتفاظ باليوم 7 | >30% | >45% |
| معدل التحويل (مجاني → مدفوع) | >3% | >8% |
| متوسط درجة STEP (المستخدمين النشطين) | +15 نقطة | +25 نقطة |
| درجة رضا المستخدم (NPS) | >40 | >60 |
| توفر المنصة | 99.9% | 99.95% |
| وقت تحميل الصفحة (p75) | <2.5s | <1.5s |

---

## ملاحظات تقنية مهمة

1. **نموذج البيانات**: `UserProfile` غير طبيعي (denormalized) للقراءة المفردة للوحة التحكم — أي تغيير في الحقول المحسوبة يتطلب تحديثاً ذرياً عبر `updateUserProfile` مع `updatedAt`.

2. **قواعد الحذف**: `firestore.rules` تمنح `owner-delete` على مجموعات محددة للسماح بالحذف المتتالي من جانب العميل. إضافة مجموعة جديدة تتطلب تحديث القواعد.

3. **المحاكيات**: CI يستخدم `firebase emulators:exec` — اختبارات E2E تعمل ضد بيانات نظيفة في كل تشغيل. التطوير المحلي: `firebase emulators:start` + `NEXT_PUBLIC_USE_FIREBASE_EMULATOR=true`.

4. **الخطوط**: العربية تستخدم Thmanyah (محلي، مرخص)، الإنجليزية تستخدم Plus Jakarta Sans (Google Fonts). العناوين العربية تسقط على خط النظام لأن خط العرض لا يحتوي على حروف عربية — هذا مقصود وموثق.

5. **التوقيت**: كل التواريخ مخزنة كـ ISO strings UTC. العرض يستخدم `Intl.DateTimeFormat` مع لغة المستخدم الحالية.

6. **الامتحان**: `useExamTimer` hook يدير العد التنازلي والإرسال التلقائي. `timeBySection` يتتبع الثواني لكل قسم للتقارير.

7. **التكرار المتباعد**: خوارزمية SM-2 في `src/lib/spaced-repetition.ts` — معامل السهولة، الفاصل، التكرار مخزنة لكل كلمة في `vocabReviewState`.

---

*آخر تحديث: 2026-08-10 — تم إنشاؤه بمراجعة شاملة للكودباس (157 ملفاً، ~15k سطر)*